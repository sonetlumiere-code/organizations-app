// import type { Organization, User as PrismaUser, Role } from "@prisma/client"

// export type Membership = {
//   organization: Pick<Organization, "id" | "name" | "slug">
//   isAdmin: boolean
//   role?: Pick<Role, "id" | "name" | "description" | "permissions">
// }

// export interface AuthUser
//   extends Pick<PrismaUser, "id" | "email" | "name" | "image"> {
//   memberships?: Membership[]
// }
